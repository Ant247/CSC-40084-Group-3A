{
  "name": "myapp-dev",
  "private": true,
  "scripts": {
    "dev:api": "cd backend && uvicorn main:app --reload --port 8000",
    "dev:web": "cd frontend && npm run dev -- --host",
    "dev": "npm-run-all -p dev:api dev:web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}